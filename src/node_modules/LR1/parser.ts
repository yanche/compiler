
import * as prod from 'productions';

export default class LR1Parser {
    private _prodset: prod.ProdSet;
    constructor(prodset: prod.ProdSet) {
        let startnont = prodset.getStartNonTerminal(), queue = [];

        this._prodset = prodset;
    }
}