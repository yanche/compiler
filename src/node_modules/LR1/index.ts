
import LR1Parser from './parser';
import * as prod from 'productions';

export function createLL1Parser(mprodarr: Iterable<string>): LR1Parser {
    return new LR1Parser(prod.createProdSet(mprodarr));
}