"use strict";
const index_1 = require('../index');
describe('SLR(1) parse', function () {
    it('simple 1', function () {
        try {
            let lr1parser = index_1.createSLR1Parser([
                'E -> T + E | T',
                'T -> int | int * T | ( E )'
            ]);
            lr1parser.print();
        }
        catch (err) {
            console.error(err.stack);
        }
    });
});
//# sourceMappingURL=parse.js.map