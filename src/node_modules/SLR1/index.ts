
import SLR1Parser from './parser';
import * as prod from 'productions';

export function createSLR1Parser(mprodarr: Iterable<string>): SLR1Parser {
    return new SLR1Parser(prod.createProdSet(mprodarr));
}