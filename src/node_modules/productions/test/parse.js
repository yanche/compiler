"use strict";
const assert = require('assert');
const utility = require('utility');
const index_1 = require('../index');
function validate(pset, expected) {
    let allLHS = pset.getNonTerminals();
    assert.equal(true, utility.arrayEquivalent([...allLHS], expected.map(function (e) { return e.lhs; })));
    for (let i = 0; i < expected.length; ++i) {
        let item = expected[i];
        assert.equal(true, utility.arrayEquivalent(item.rhsArr, pset.getRHSArr(item.lhs), function (test, real) {
            if (test.length !== real.length)
                return false;
            for (let q = 0; q < test.length; ++q) {
                let titem = test[q], symbol = real[q];
                if (titem.str !== symbol.getName() || titem.terminal !== symbol.isTerminal())
                    return false;
            }
            return true;
        }));
    }
}
;
describe('parse string into structured production set', function () {
    it('simple 1', function () {
        let pset = index_1.createProdSet([
            'E -> T + E | T',
            'T -> int | int * T | ( E )'
        ]);
        assert.equal('E', pset.getStartNonTerminal());
        validate(pset, [
            {
                lhs: 'E',
                rhsArr: [
                    [
                        { str: 'T', terminal: false },
                        { str: '+', terminal: true },
                        { str: 'E', terminal: false }
                    ],
                    [
                        { str: 'T', terminal: false }
                    ]
                ]
            }, {
                lhs: 'T',
                rhsArr: [
                    [
                        { str: 'int', terminal: true },
                        { str: '*', terminal: true },
                        { str: 'T', terminal: false }
                    ],
                    [
                        { str: '(', terminal: true },
                        { str: 'E', terminal: false },
                        { str: ')', terminal: true }
                    ],
                    [
                        { str: 'int', terminal: true }
                    ]
                ]
            }
        ]);
    });
    it('simple 2', function () {
        let pset = index_1.createProdSet([
            'E -> int + int | '
        ]);
        assert.equal('E', pset.getStartNonTerminal());
        validate(pset, [
            {
                lhs: 'E',
                rhsArr: [
                    [
                        { str: 'int', terminal: true },
                        { str: '+', terminal: true },
                        { str: 'int', terminal: true }
                    ],
                    []
                ]
            }
        ]);
    });
    it('not all non-terminal appears at LHS', function () {
        assert.throws(function () {
            return index_1.createProdSet([
                'E -> T | int'
            ]);
        }, Error);
    });
});
//# sourceMappingURL=parse.js.map