"use strict";
const utility = require('utility');
class NodeNumMap {
    constructor() {
        this._nodenummap = new Map();
        this._numnodemap = new Map();
        this._idGen = new utility.IdGen();
    }
    getOrCreateNum(name) {
        if (this._nodenummap.has(name))
            return this._nodenummap.get(name);
        else {
            let id = this._idGen.next();
            this._nodenummap.set(name, id);
            this._numnodemap.set(id, name);
            return id;
        }
    }
    getNode(num) {
        return this._numnodemap.get(num);
    }
}
class Symbol {
    constructor(terminal, name) {
        if (name === '$')
            throw new Error('$ is reserved as a symbol');
        this._terminal = terminal;
        this._name = name;
    }
    isTerminal() { return this._terminal; }
    getName() { return this._name; }
}
exports.Symbol = Symbol;
class Production {
    constructor(lhs, rhs) {
        if (lhs.isTerminal())
            throw new Error('left-hand-side of production must be non-terminal');
        this._lhs = lhs;
        this._rhs = rhs || [];
        this._id = this._lhs.getName() + ' -> ' + this._rhs.map(r => r.getName()).join(' ');
    }
    getLHS() { return this._lhs; }
    getRHS() { return this._rhs; }
    getId() { return this._id; }
    print() { console.log(this._id); return this; }
}
exports.Production = Production;
class ProdSet {
    constructor(prods) {
        let prodsize = 0, terminals = new Set(), nonterminals = new Set();
        let prodidset = new Set(), nodenummap = new NodeNumMap(), prodmap = new Map();
        let startnonterminal = null, reachedges = [];
        for (let prod of prods) {
            let prodid = prod.getId();
            if (prodidset.has(prodid))
                throw new Error('duplicate production is not allowed: ' + prodid);
            prodidset.add(prodid);
            let lname = prod.getLHS().getName();
            let lnum = nodenummap.getOrCreateNum(lname);
            if (prodsize === 0)
                startnonterminal = lname;
            let rhs = prod.getRHS();
            addToMapOfArr(prodmap, lname, rhs);
            nonterminals.add(lname);
            for (let symbol of rhs) {
                let rname = symbol.getName();
                if (symbol.isTerminal())
                    terminals.add(rname);
                else {
                    nonterminals.add(rname);
                    reachedges.push({ src: lnum, tgt: nodenummap.getOrCreateNum(rname) });
                }
            }
            ++prodsize;
        }
        if (prodmap.size !== nonterminals.size)
            throw new Error('not all non-terminals appear at LHS');
        if (utility.closure.calcClosureOfOneNode(reachedges, nodenummap.getOrCreateNum(startnonterminal)).size !== nonterminals.size)
            throw new Error('some non-terminals are unreachable from start symbol');
        this._terminals = terminals;
        this._nonterminals = nonterminals;
        this._startnonterminal = startnonterminal;
        this._prodidset = prodidset;
        this._prodmap = prodmap;
    }
    getStartNonTerminal() { return this._startnonterminal; }
    getNonTerminals() { return this._nonterminals; }
    getTerminals() { return this._terminals; }
    getRHSArr(lhs) { return this._prodmap.get(lhs); }
    print() { for (let prodid of this._prodidset)
        console.log(prodid); return this; }
    firstSet() {
        if (this._firstSet != null)
            return this._firstSet;
        let nullablenont = this.nullableNonTerminals(), retmap = new Map();
        let nodenummap = new NodeNumMap(), edges = [], nonterminals = this.getNonTerminals();
        for (let nont of nonterminals) {
            retmap.set(nont, new Set());
            for (let rsyms of this.getRHSArr(nont)) {
                let genepsilon = true, loc = 0;
                while (loc < rsyms.length && genepsilon) {
                    let rsym = rsyms[loc];
                    let rname = rsym.getName();
                    if (rsym.isTerminal()) {
                        addToMapOfSet(retmap, nont, rname);
                        genepsilon = false;
                    }
                    else {
                        let lnum = nodenummap.getOrCreateNum(nont), rnum = nodenummap.getOrCreateNum(rname);
                        edges.push({ src: lnum, tgt: rnum });
                        genepsilon = nullablenont.has(rname);
                        ++loc;
                    }
                }
            }
        }
        mergeClosureSet(nonterminals, nodenummap, utility.closure.calcClosure(edges), retmap);
        for (let t of this.getTerminals())
            retmap.set(t, new Set().add(t));
        this._firstSet = retmap;
        return retmap;
    }
    followSet() {
        if (this._followSet != null)
            return this._followSet;
        let retmap = new Map(), firstSetMap = this.firstSet(), nullablenonterminals = this.nullableNonTerminals();
        let nodenummap = new NodeNumMap(), edges = [], nonterminals = this.getNonTerminals();
        addToMapOfSet(retmap, this._startnonterminal, '$');
        for (let nont of nonterminals) {
            if (!retmap.has(nont))
                retmap.set(nont, new Set());
            for (let symarr of this.getRHSArr(nont)) {
                if (symarr.length === 0)
                    continue;
                let i = 0, j = 0;
                while (i < symarr.length) {
                    let iname = symarr[i].getName();
                    let fset = firstSetMap.get(iname);
                    let s = j;
                    while (s < i)
                        addRangeToMapOfSet(retmap, symarr[s++].getName(), fset);
                    if (!nullablenonterminals.has(iname))
                        j = i;
                    ++i;
                }
                let lnum = nodenummap.getOrCreateNum(nont);
                while (j < i) {
                    let rnum = nodenummap.getOrCreateNum(symarr[j].getName());
                    if (rnum != lnum)
                        edges.push({ src: rnum, tgt: lnum });
                    ++j;
                }
            }
        }
        let clomap = utility.closure.calcClosure(edges);
        mergeClosureSet(nonterminals, nodenummap, clomap, retmap);
        mergeClosureSet(this.getTerminals(), nodenummap, clomap, retmap);
        this._followSet = retmap;
        return retmap;
    }
    nullableNonTerminals() {
        if (this._nullableNonTerminals != null)
            return this._nullableNonTerminals;
        let retset = new Set(), queue = [];
        let map = new Map();
        for (let nont of this.getNonTerminals()) {
            for (let rsym of this.getRHSArr(nont)) {
                if (rsym.length === 0) {
                    if (!retset.has(nont)) {
                        retset.add(nont);
                        queue.push(nont);
                    }
                    break;
                }
                if (rsym[0].isTerminal())
                    continue;
                addToMapOfArr(map, rsym[0].getName(), { rsym: rsym, next: 1, lstr: nont });
            }
        }
        while (queue.length > 0) {
            let lstr = queue.pop();
            for (let ditem of (map.get(lstr) || [])) {
                if (retset.has(ditem.lstr))
                    continue;
                let next = ditem.next;
                while (next < ditem.rsym.length && retset.has(ditem.rsym[next].getName()))
                    ++next;
                if (next === ditem.rsym.length) {
                    retset.add(ditem.lstr);
                    queue.push(ditem.lstr);
                }
                else {
                    let sym = ditem.rsym[next];
                    if (sym.isTerminal())
                        continue;
                    addToMapOfArr(map, sym.getName(), { rsym: ditem.rsym, next: next + 1, lstr: ditem.lstr });
                }
            }
        }
        this._nullableNonTerminals = retset;
        return retset;
    }
    leftFactoredProdSet() {
        let prods = [], idgen = new utility.IdGen();
        for (var nont of this.getNonTerminals()) {
            leftFactoring(nont, this.getRHSArr(nont), 0, prods, idgen);
        }
        return prods.length === this._prodidset.size ? this : new ProdSet(prods);
    }
}
ProdSet.preservedNonTerminalPrefix = 'D__';
exports.ProdSet = ProdSet;
function leftFactoring(lstr, rhsarr, lfidx, prods, idGen) {
    var lfmap = new Map(), gonull = false, lfset = new Set();
    for (let rsymarr of rhsarr) {
        if (rsymarr.length === lfidx)
            gonull = true;
        else {
            let lfname = rsymarr[lfidx].getName();
            addToMapOfArr(lfmap, lfname, rsymarr);
            lfset.add(lfname);
        }
    }
    let lsymbol = new Symbol(false, lstr);
    if (gonull)
        prods.push(new Production(lsymbol, []));
    for (let lfname of lfset) {
        let rights = lfmap.get(lfname);
        if (rights.length === 1)
            prods.push(new Production(lsymbol, lfidx > 0 ? rights[0].slice(lfidx) : rights[0]));
        else {
            let goahead = false, i = 1, lfidx0 = lfidx, anchor;
            lfidx -= 1;
            do {
                lfidx += 1;
                i = 1;
                if (rights[0].length > lfidx + 1) {
                    anchor = rights[0][lfidx + 1].getName();
                    while (i < rights.length && (rights[i].length > lfidx + 1) && rights[i][lfidx + 1].getName() === anchor)
                        ++i;
                }
            } while (i === rights.length);
            var newnont = ProdSet.preservedNonTerminalPrefix + idGen.next();
            prods.push(new Production(lsymbol, rights[0].slice(lfidx0, lfidx + 1).concat(new Symbol(false, newnont))));
            leftFactoring(newnont, rights, lfidx + 1, prods, idGen);
        }
    }
}
function addToMapOfArr(map, key, item) {
    if (!map.has(key))
        map.set(key, [item]);
    else
        map.get(key).push(item);
}
function addToMapOfSet(map, key, item) {
    if (!map.has(key))
        map.set(key, new Set().add(item));
    else
        map.get(key).add(item);
}
function addRangeToMapOfSet(map, key, item) {
    let set = map.get(key);
    if (set == null) {
        set = new Set();
        map.set(key, set);
    }
    for (let s of item)
        set.add(s);
}
function mergeClosureSet(symstrarr, nodenummap, closuremap, map) {
    for (let str of symstrarr) {
        let snum = nodenummap.getOrCreateNum(str);
        let closure = closuremap.get(snum);
        if (closure == null)
            continue;
        for (let num of closure.getNodes()) {
            if (num === snum)
                continue;
            let symset = map.get(nodenummap.getNode(num));
            if (symset == null)
                return;
            for (let s of symset)
                addToMapOfSet(map, str, s);
        }
        ;
    }
    ;
}
//# sourceMappingURL=production.js.map