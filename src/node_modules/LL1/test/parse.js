"use strict";
const index_1 = require('../index');
describe('LL(1) parse', function () {
    it('simple 1', function () {
        let ll1parser = index_1.createLL1Parser([
            'E -> T X',
            'X -> + E | ',
            'T -> int Y | ( E )',
            'Y -> | * T'
        ]);
        let parseret = ll1parser.parse([
            { rawstr: '1', symbol: 'int' },
            { rawstr: '+', symbol: '+' },
            { rawstr: '2', symbol: 'int' },
            { rawstr: '*', symbol: '*' },
            { rawstr: '3', symbol: 'int' },
        ]);
        console.info(parseret.accept);
        console.info(parseret.root);
    });
});
//# sourceMappingURL=parse.js.map