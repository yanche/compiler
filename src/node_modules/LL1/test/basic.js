
var LL1 = require('LL1');
var utility = require('utility');
var prod = require('productions');

describe('simple test at parsing table creation of LL(1) grammar', function () {
    it('simple 1', function () {
        // var prodset = prod.createProdSet([
        //     'E -> T X',
        //     'X -> + E | ',
        //     'T -> int Y | ( E )',
        //     'Y -> | * T'
        // ]);
        var parser = LL1.createParser([
            'E -> T + E | T',
            'T -> int | int * T | ( E )'
        ]);
        parser.print();
    });
});
