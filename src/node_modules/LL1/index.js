
var productions = require('productions');
var Parser = require('./parser.js');

var createParser = function (arrOfMPDS) {
    var prodset = productions.createProdSet(arrOfMPDS);
    return new Parser(prodset);
};

exports.createParser = createParser;
